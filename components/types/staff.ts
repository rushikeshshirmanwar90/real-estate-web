export type StaffProps = {
  id: string;
  srNumber: number;
  name: string;
  email: string;
  phone: string;
};

export interface StaffDataProps {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  userType: string;
  clientId: string;
}
