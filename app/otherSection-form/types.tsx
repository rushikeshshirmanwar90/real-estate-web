export interface Amenity {
    icon: string;
    name: string;
}
export interface OtherSectionProps {
    name: string;
    description?: string;
    projectId: string;
    area: number;
    images: string[];
}